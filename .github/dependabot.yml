version: 2
updates:
  # Keep the GitHub Actions we pin (@v4/@v5, pypi-publish) current.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "CI"
